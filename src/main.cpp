#include "Automata.h"
#include <iostream>

int main() {
    Automata automata;

    automata.on();

    automata.coin(200);

    automata.etMenu("menu.txt");

    automata.getState();

    automata.choice(0);

    automata.check();

    automata.cook();

    automata.finish();

    return 0;
}
